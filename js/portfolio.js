var holder;

function checkMe(){
  holder = $(window).height()
  console.log(holder);
  holder = 0;
};

function positionMe(element){
  var hold = element.attr()
}

var elementToPosition = $('#drop-text-container');
